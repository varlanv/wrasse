package sample

import sample.auxop.Vec
import sample.auxop.component1
import sample.auxop.component2
import sample.auxop.plus

val a = Vec(1, 2)
val b = Vec(3, 4)
val c = a + b

fun run() {
    val (px, py) = a
}