{
    "$schema": "http://json-schema.org/draft-07/schema#",
    "title": "Wrasse Configuration",
    "description": "Configuration file for the Wrasse Kotlin compiler plugin",
    "type": "object",
    "additionalProperties": false,
    "properties": {
        "$schema": {
            "type": "string"
        },
        "extends": {
            "type": "string",
            "description": "Path to a base config file (relative to this file). Child values override base values."
        },
        "exclude": {
            "type": "array",
            "description": "Glob patterns for files to exclude from all rules",
            "items": {
                "type": "string"
            }
        },
        "rules": {
            "type": "object",
            "description": "Rule configurations. Absent rules default to off (or inherit from base via extends).",
            "additionalProperties": false,
            "properties": {
                "if-else-bracing": {
                    "$ref": "#/$defs/ruleConfig",
                    "description": "Require braces around a bare if/else branch belonging to a multi-line if-statement"
                },
                "modifier-order": {
                    "$ref": "#/$defs/ruleConfig",
                    "description": "Require modifiers to appear in the Kotlin coding-conventions canonical order"
                },
                "no-empty-class-body": {
                    "$ref": "#/$defs/ruleConfig",
                    "description": "Disallow class/interface/object/enum bodies containing nothing but whitespace"
                },
                "no-empty-parens-before-trailing-lambda": {
                    "$ref": "#/$defs/ruleConfig",
                    "description": "Disallow an empty '()' argument list immediately before a trailing lambda"
                },
                "no-semicolons": {
                    "$ref": "#/$defs/ruleConfig",
                    "description": "Disallow unnecessary semicolons"
                },
                "no-unit-return": {
                    "$ref": "#/$defs/ruleConfig",
                    "description": "Disallow a redundant explicit ': Unit' return type on a block-body function"
                },
                "redundant-visibility-modifier": {
                    "$ref": "#/$defs/ruleConfig",
                    "description": "Disallow a redundant explicit 'public' visibility modifier on a class, function, or property"
                },
                "import-ordering": {
                    "$ref": "#/$defs/ruleConfig",
                    "description": "Require import directives to be in plain ASCII-alphabetical order"
                },
                "no-unused-imports": {
                    "$ref": "#/$defs/ruleConfig",
                    "description": "Disallow imports whose target is never referenced in the file"
                },
                "no-wildcard-imports": {
                    "$ref": "#/$defs/ruleConfig",
                    "description": "Disallow wildcard imports"
                },
                "no-unnecessary-fqn": {
                    "$ref": "#/$defs/ruleConfig",
                    "description": "Disallow fully qualified names that could be dropped given an existing or addable import (report-only)"
                },
                "trailing-newline": {
                    "$ref": "#/$defs/ruleConfig",
                    "description": "Require a trailing newline at end of file"
                },
                "when-entry-bracing": {
                    "$ref": "#/$defs/ruleConfig",
                    "description": "Require braces around a bare when-entry body when some sibling entry is already braced and some entry has a multiline body"
                }
            }
        }
    },
    "$defs": {
        "ruleConfig": {
            "type": "object",
            "additionalProperties": false,
            "properties": {
                "level": {
                    "type": "string",
                    "enum": [
                        "off",
                        "warn",
                        "error"
                    ],
                    "description": "Rule severity: 'off' disables, 'warn' reports as warning, 'error' reports as error"
                },
                "exclude": {
                    "type": "array",
                    "description": "Additional glob patterns to exclude for this rule",
                    "items": {
                        "type": "string"
                    }
                }
            }
        }
    }
}
